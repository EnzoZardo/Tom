tipo wrapping = { value: inteiro; wrap: (inteiro): objeto  } 

fun wrap(n: inteiro): wrapping
{
    fun w(x: inteiro): wrapping {
        retorne wrap(n + x)
    }

    retorne { value: n, wrap: w(1) }
}

escreva(wrap(2))