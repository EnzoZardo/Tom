package Entities.Enums.Lexer;

public enum TokenType
{
    IF,
    NEW,
    DOT,
    EOF,
    FOR,
    EACH,
    ELSE,
    TYPE,
    BREAK,
    WHILE,
    COLON,
    COMMA,
    CLASS,
    EQUALS,
    RETURN,
    DECLARE,
    CONSTANT,
    CONTINUE,
    FUNCTION,
    SEMICOLON,
    IDENTIFIER,
    OPEN_BRACE,
    CLOSE_BRACE,
    OPEN_BRACKETS,
    INTERROGATION,
    FLOAT_LITERAL,
    STATIC_MARKER,
    CLOSE_BRACKETS,
    STRING_LITERAL,
    INTEGER_LITERAL,
    BINARY_OPERATOR,
    OPEN_PARENTHESIS,
    PROTECTION_MARKER,
    CLOSE_PARENTHESIS,
}
